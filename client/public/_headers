/*
  # CSP is set dynamically by functions/_middleware.ts (per-request nonce).
  # Do NOT add a Content-Security-Policy line here — it would duplicate/conflict
  # with the middleware header.
  Referrer-Policy: strict-origin-when-cross-origin
